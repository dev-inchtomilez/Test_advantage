
  # advantedge

  This is a code bundle for advantedge. The original project is available at https://www.figma.com/design/0a3XpwvuS4ZninjLx3INby/advantedge.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  